# 可选：仅当无法使用 config/config.json 时作临时覆盖（生产请用 CONFIG_JSON_PATH）
# 主配置与 Java 共用：config/config.json 或 D:/archive/config/config.json
#
# CONFIG_JSON_PATH=D:/archive/config/config.json
# GLM_API_KEY=          # 覆盖 glm.apiKey
# MYSQL_PASSWORD=       # 覆盖 database.password
