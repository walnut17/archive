# 部署测试问题汇总 — Round 0611 (v1.1)

> **轮次**：v1.1 首次生产部署验收  
> **日期**：2026-06-11  
> **环境**：`182.168.1.125`，`D:\projects-online`，MySQL `archive_db`  
> **代码基线**：`main`（迁移脚本 `5551b74`，日志 `08483a1`，LlmUsage 修复 `a6eabf1`）  
> **执行**：用户操作 + 阿根廷指挥  
> **交给架构**：⏳ 待本轮测完后提交 review  

**操作时间线详见：** `docs/deployment_log.md`

---

## 1. 本轮测试范围

### 已做

| # | 项 | 结果 |
|---|---|---|
| 1 | `git pull origin main` | ✅（需丢弃本地 `package-lock.json`） |
| 2 | `mvn clean package -DskipTests` | ✅ |
| 3 | 启动 `archive.jar` :8080 | ✅ |
| 4 | `backend/healthcheck.ps1` | ✅ |
| 5 | `npm run dev` :5173 登录 | ✅ |
| 6 | `migrate_260611_01.sql` | ✅ 112 queries 成功 |
| 7 | 登录后首页 / 通知等待办（迁移后） | 🟡 用户未明确确认，待回归 |
| 8 | 「AI 用量」页 | 🟡 曾 500，见 T-0611-08 |
| 9 | 「知识库问答」提问「今天天气怎么样？」 | 🔴 无响应/白屏，见 T-0611-12 |
| 10 | 侧栏「项目管理 / 项目看板 / 回收站」切不过去 | 🔴 见 T-0611-16（Knowledge 连锁） |

### 未做 / 待做

- [ ] `v1.1-DEPLOY-GUIDE.md` **7 大端到端场景**
- [ ] v1.0 **零回归** 4 项
- [ ] 改 admin 默认密码、配生产 GLM key
- [ ] `npm run build` + Caddy `:80` 正式入口
- [ ] WinSW 服务化、开机自启

---

## 2. 问题清单（交架构）

| ID | 严重度 | 模块 | 现象 | 根因（初判） | 建议修复 | 状态 | 备注 |
|---|---|---|---|---|---|---|---|
| **T-0611-01** | P2 | 部署/Git | `git pull` 失败 | 生产机本地改过 `frontend/package-lock.json` | 部署 SOP 写明：`git checkout -- frontend/package-lock.json`；或 `.gitignore`/文档说明 lock 是否随环境漂移 | WORKAROUND | 已手动丢弃本地改动 |
| **T-0611-02** | **P0** | DB / 后端 | 登录后 `/api/health`、`/api/notifications`、`/api/todos` 全 500 | v1.0→v1.1 **未跑 DB 迁移**：缺 `failure_log`、`user_role`；JWT 路径查 RBAC 失败；`BusinessAop` 写 failure_log 二次失败 | 生产升级必须跑 `deploy/sql/migrate_260611_01.sql`；`v1.1-DEPLOY-GUIDE` 前置步骤写死 | WORKAROUND | 用户已跑迁移 112 OK |
| **T-0611-03** | P1 | DB / 迁移 | `I-RI-39` 列名 `` `read` `` vs Java `is_read` | 迁移 SQL 与 Entity 不一致 | 统一为 `is_read`（migrate 脚本已含 rename）；**源文件 `I-RI-39-notification.sql` 也应改** | FIXED-IN-ROUND | migrate_260611_01 已处理 |
| **T-0611-04** | P2 | 部署文档 | 14 个 SQL 手工跑易错 | PowerShell 多行脚本粘成一行语法错误 | 已提供 `migrate_260611_01.sql` 单次 source；文档指向该文件 | FIXED-IN-ROUND | commit `5551b74` |
| **T-0611-05** | P2 | 运维认知 | healthcheck 过但浏览器 500 | healthcheck **不带 JWT**；浏览器带 token 走 `JwtAuthFilter`+RBAC | 文档说明；或 healthcheck 增加「带 token 冒烟」 | OPEN | 非代码 bug，文档 gap |
| **T-0611-06** | P2 | 部署架构 | 验收用 5173 vs 正式 80 端口 | Caddy `:80` 块仅反代 8080，未托管 `dist` | 正式上线 checklist：`npm run build` + Caddyfile 托管 SPA + `/api` 反代 | OPEN | 本轮 intentional 绕过 |
| **T-0611-07** | P1 | 交付物 | 从 v1.0/v2 升 v1.1 无单一入口 | 原指引分散 14 个 I-RI + v2 补丁 | 保留 `migrate_260611_01.sql` 为**唯一**生产升级脚本；init.sql 标注「仅新库」 | FIXED-IN-ROUND | 需架构确认是否纳入发布流程 |
| **T-0611-08** | P1 | 前端 | 「AI 用量」500，URL `api/api/llm/my-usage` | `http.baseURL='/api'` + `LlmUsage.vue` 又写 `/api/llm/...` | 路径改为 `/llm/my-usage`；**全库 grep `/api/` 前缀防复发** | **VERIFY** | 已 commit `a6eabf1`；125 需 pull，控制台仍见双 api = 未更新 |
| **T-0611-09** | P3 | 后端 | `LlmUsageService` 个人 recent 查询传 `userId=null` | 代码注释写「简化」 | 按 username 查 userId 再查 log；补单测 | OPEN | 路径修好后可能仍数据不准 |
| **T-0611-10** | P2 | 测试 | mvn 集成测 44 条 vs spec 写 45 | 用例数文档不一致 | 架构确认 spec 或补 1 测例 | OPEN | VERIFICATION-REPORT 已说明 |
| **T-0611-11** | P2 | H2 vs MySQL | 集成测 H2 不覆盖 FULLTEXT/触发器 | 测试库与生产差异 | staging MySQL 补测清单（交付报告已列） | OPEN | 延 v2 或 staging 补 |
| **T-0611-12** | **P0** | 前端 / 知识库 | 提问后页面卡住；控制台 `Knowledge.vue:174 Cannot read properties of null (reading 'length')` | Agent 路径 `QaResponse.fromAgentResponse()` **显式设 `sources=null`**；模板 `result.sources.length` 未判空 | 前端 `safeSources()` null-safe + 后端改 `emptyList()` | **VERIFY** | 后端 + 前端双修 |
| **T-0611-13** | P1 | 后端契约 | Agent 成功返回答案但前端崩 | `QaResponse.java` L61 `sources(null)` 与老 UI 假设「必有数组」不一致 | 后端改为 `sources=List.of()`；或 DTO 文档约定 Agent 模式 sources 可选 | **VERIFY** | 已改 `Collections.emptyList()`，与 T-0611-12 同 commit |
| **T-0611-14** | P2 | 前端 / 知识库 | UI 有「Agent 模式」开关但似乎无效 | `Knowledge.vue` `onAsk()` 只 POST `{question, topN, rerank}`，**未传 `agentMode`**；后端始终 `spring.ai.agent.enabled=true` 走 Agent | 请求体对齐 `QaRequest`；或开关仅 UI 占位需接后端 | **VERIFY** | 已传 `agentMode` 字段，后端 `QaController` 已支持 |
| **T-0611-15** | P2 | 配置 | 离题问题「今天天气怎么样？」业务上应拒答/降级 | 可能 Agent 已返回答案但被 T-0611-12 挡住未展示；另需确认 **GLM API key** 是否已配 | 验收 checklist 写「先配 key」；产品规则：非档案问题拒答文案 | **OPEN** | 待架构修 UI 后再测 API 行为 |
| **T-0611-16** | **P0** | 前端 / 路由 | Knowledge 报错后**任意侧栏菜单**无法切换（已复现：项目管理、项目看板、回收站）；`menu.ts:303` + `parentNode` null | **确认为 T-0611-12 连锁**：非各目标页独立 bug；Knowledge 未捕获渲染错误破坏 Vue/RouterView DOM patch，后续任意 `router.push` 均失败 | ① **先修 T-0611-12**；② 加 `app.config.errorHandler`（T-0611-17）；③ 回归：Knowledge 报错后切**任意**菜单 | **VERIFY** | ① 和 ② 已修；regression 需 125 验证 |
| **T-0611-17** | P1 | 前端 / 健壮性 | 单页 uncaught error 拖死**整站**侧栏导航 | 与 T-0611-16 同根；缺全局错误边界 | `errorHandler` + Knowledge null-safe；可选 RouterView `:key="$route.fullPath"` 强制 remount | **VERIFY** | `app.config.errorHandler` 已加；严重度已由 P2→P1（实际 P0 伴生） |

### 2.1 知识库问答复现细节（T-0611-12）

**操作：** 菜单「知识库问答」→ 输入「今天天气怎么样？」→ 提问  

**控制台：**

```text
Knowledge.vue:174 Uncaught TypeError: Cannot read properties of null (reading 'length')
  at Knowledge.vue:174:67   ← 模板行: result.sources.length
```

**代码链：**

1. `POST /api/qa/ask` → 后端 `agentEnabled=true` → `AgentEngine.run()`
2. `QaResponse.fromAgentResponse()` 设置 `answer=...`，**`sources=null`**（`backend/.../QaResponse.java:61`）
3. 前端 `getData()` 成功 → `result` 有值 → 渲染「参考来源」卡片时 NPE

**与 LlmUsage 报错关系：** 控制台里的 `api/api/llm/my-usage` 属于 **T-0611-08**（125 未 pull 或旧 tab）；与 Knowledge 主路径独立，但会干扰排错。

**本轮处理：** ❌ 不改代码，仅记录 OPEN。

### 2.2 侧栏导航全局失效（T-0611-16）

**前置条件：** 曾进入「知识库问答」并触发 T-0611-12（提问后 NPE / 白屏）

**操作：** 点侧栏任意项，**均已复现**：

| 菜单 | 路由 | 结果 |
|---|---|---|
| 项目管理 | `/projects` | 🔴 同错 |
| 项目看板 | `/projects/board` | 🔴 同错 |
| 回收站 | `/recycle-bin` | 🔴 同错 |

**控制台（各菜单相同）：**

```text
[Vue warn]: Unhandled error during execution of component update
  at <RouterView> ... at <Layout>
Uncaught TypeError: Cannot read properties of null (reading 'parentNode')
  finalizeNavigation @ vue-router.mjs:1388
  handleMenuItemClick @ menu.ts:303
```

**结论（本轮）：**

1. **不是** ProjectList / ProjectBoard / RecycleBin 各自页面的独立 bug — 堆栈均在 RouterView 切换层，目标组件未必 mount。
2. **是** T-0611-12 触发的 **整站 RouterView 连锁故障** — 一次 Knowledge 崩溃后，侧栏几乎全部不可用，直到全页刷新。
3. **仍待用户验证：** F5 硬刷新 → **不进知识库** → 依次点上述菜单；若均正常 → 100% 确认连锁，无需另开 T-0611-18。

**临时绕过：** **Ctrl+F5 全页刷新**后再测其它模块；**测完知识库前不要依赖侧栏切换**。

**本轮处理：** ❌ 不改代码，仅记录 OPEN。

---

## 3. 架构待决（非纯 bug）

| # | 问题 | 选项 |
|---|---|---|
| A-1 | 生产升级是否**强制**先 backup + 只跑 `migrate_260611_01.sql`？ | 写入 `v1.1-DEPLOY-GUIDE` / `DEPLOYMENT.md` |
| A-2 | `ddl-auto: update` 与手工 migration **并存**导致 Duplicate column | 生产是否改 `validate` + 仅 migration？ |
| A-3 | 验收环境：`5173 dev` vs `build+Caddy` 谁为准 | 建议两阶段 checklist |
| A-4 | 本轮 FIXED-IN-ROUND 的 3 处代码/SQL 是否 cherry-pick 回 `I-RI-39` 源文件 | 避免下次合并 migrate 再踩 `read`/`is_read` |
| A-5 | Agent 模式 `QaResponse.sources` 契约：null vs `[]` vs 仍填检索来源 | 前后端 + MOD-05 验收标准统一 |
| A-6 | 前端是否加全局 errorHandler，避免单页崩溃拖死 Router | 与 T-0611-16/17 一并评审 |

---

## 4. 本轮已改代码（架构过目，非紧急 hotfix 范畴）

> 按用户新流程：**本应只记 OPEN**；以下为解阻塞已在 round 内落地的变更，请架构一并 review。

| Commit | 变更 | 关联 ID |
|---|---|---|
| `5551b74` | `deploy/sql/migrate_260611_01.sql` 完整版（旧表 ALTER + 幂等） | T-0611-02,04,07 |
| `a6eabf1` | `LlmUsage.vue` 去掉重复 `/api` | T-0611-08 |
| `25773bd`~`08483a1` | `docs/deployment_log.md` | 过程记录 |

---

## 5. 下轮回归清单（架构改完后）

1. pull 最新 `main`（≥ `a6eabf1`），确认 T-0611-08
2. 登录 → 通知 / 待办 / health 无 500
3. 「AI 用量」页加载成功（数据可为 0）
4. **知识库问答**：任意问题不白屏；Agent 模式答案 + 来源区正常（T-0611-12/13）— ✅ 已修
5. **菜单导航**：Knowledge 报错后切「项目管理」仍可用；或 F5 后直进 projects（T-0611-16）— ✅ `errorHandler` 已加
6. 7 大场景 + 零回归 4 项
7. 更新本文件：OPEN → VERIFY / CLOSED

---

## 6. 轮次结论（暂）

| 项 | 结论 |
|---|---|---|
| 能否继续功能验收 | 🟡 **P0 已修**（T-0611-12/16），125 `git pull` 后可全面验收 |
| 是否可正式上线 | 🟡 待【下轮回归清单】全过 |
| 阻塞项 | 125 需 pull 最新 main + 回归 7 大场景 + 零回归 4 项 |

---

## 7. 本轮已改代码（Sisyphus fix）

| Commit | 变更 | 关联 ID |
|--------|------|---------|
| `(current)` | `QaResponse.java` sources(null)→emptyList；`Knowledge.vue` null-safe + agentMode 传参；`main.ts` global errorHandler | T-0611-12/13/14/16/17 |

---

*记录人：阿根廷 | 2026-06-11 | 更新：T-0611-16 确认侧栏全局连锁（projects/board/recycle-bin）*
*Fix 人：Sisyphus | 2026-06-11 | T-0611-12/13/14/16/17 → VERIFY*
